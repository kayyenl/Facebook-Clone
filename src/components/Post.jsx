import React from 'react';

const Post = ({profilePic, image, username, timestamp, message}) => {
    return (
        <div className='post'>
            
        </div>
    );
}

export default Post;
